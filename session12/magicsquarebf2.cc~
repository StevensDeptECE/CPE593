int main() {
	int n = 3;
	int rowsum = n * (n*n+1) / 2; // 15
	
	for (int a = 1; a <= 9; a++) {
		if (int b = 1; b <= 9; b++) {
			if (a == b)
				continue;
			if (a+b+1 > rowsum)
				continue;
			if (a + b +9< rowsum)
				continue;
			
