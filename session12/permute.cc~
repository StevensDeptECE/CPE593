#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main() {
  int x[] = {1, 2, 3, 4};
  char s[] = "abcd";
  vector<int> a = {1, 2, 3, 4};

  while (next_permutation(x, x+4)) {
    for (int i = 0; i < 4; i++)
      cout << x[i] << ' ';
    cout << '\n';
  }
    
  
