int main() {
    // given number between 1 and n --> maps to string


    unordered_map<int, string> findMyString;

    string findMyString[size];


    // given: number between -a and b, sequential
    // how do you use an array????

    string findMyString[b + abs(a)];


    // given; number betwee a and b in steps of k   ex. a = 10^9, b = 10^9 + 10^6 k = 6
    // 1000000000, 1000000006, 1000000012, ...
    // array[(i-a)/k]  // LINEARIZE THE INPUT, think of that as your hash function



    // given number between a and b, sparse (ie most numbers not chosen)
    // a = 10^9, b = 10^9 + 10^7 
    // USE AN ARRAY

    // ONLY USE HASHMAP if you cannot afford the full space of the array.

    //This should be a hash map: --> unordered_map<uint64_t, int>


    // given number between a and b
    // 1000000000
    // 2000000000
    // 4000000000
    // 8000000000
    // 1600000000

    // use an array[log(i-a)]


}