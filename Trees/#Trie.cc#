class Trie {
	class Node {
		Node*next[26];
		bool isWord;
	};
	Node root;
public:
	// n = # of symbols in trie and k is length of the string O(k)
	void add(string word) { // "cat"  c --> 2
		Node* p = &root;
		// word[0]
		for (int i = 0; i < word.length(); i++)
			word[i]

	}
};